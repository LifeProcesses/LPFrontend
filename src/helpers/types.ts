/* eslint-disable no-unused-vars */

export enum StudentStatusType {
    Accepted = 'accepted',
    Issued = 'issued',
    Interview = 'interview',
    Refused = 'refused',
    Empty = 'empty',
}

export enum InterviewStatusType {
    Accepted = 'accepted',
    Issued = 'issued',
    Interview = 'interview',
    Refused = 'refused',
}
